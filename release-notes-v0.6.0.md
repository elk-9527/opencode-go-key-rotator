## Key Router v0.6.0

- 修正 Hermes Windows 默认配置目录，并支持 `HERMES_HOME`
- 支持 Continue 多模型选择及 `${{ secrets.NAME }}` + `.env` 安全轮换
- 按 dsh 与 Pi 的真实凭据优先级保护环境变量、登录令牌和命令取密配置
- 支持 OpenCode Go / Zen，并避免覆盖扩展的 BYOK 托管状态
- 支持多套 VS Code 用户数据与扩展目录，安装和验证绑定到确认位置
- 备份改为当前 Windows 用户可解密的 DPAPI 加密文件，并提供手动恢复命令
- 增加跨进程锁、预览状态摘要、写后验证和失败回滚
- 加固本地页面的访问令牌、来源校验、一次性令牌与内容安全策略
- 安装器固定官方来源，增加重定向、哈希、ZIP 资源与路径安全校验
- 关闭窗口时等待正在进行的写入或安装安全结束
- 修复英文区域 Windows 控制台输出中文时可能崩溃的问题
