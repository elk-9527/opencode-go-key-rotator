# Key Router v0.6.0 全量复核报告

复核日期：2026-09-04

## 结论

已完成源码、配置语义、写入回滚、本地页面安全、安装器、打包产物和真实桌面发现链路的重新检查。所有可执行验收均通过，最终交付二进制为 `Key-Router-v0.6.0.exe`。

## 本轮主要修正

- 对齐 Hermes、Continue、dsh、Claude Code、Pi 和四类 VS Code 目标的真实配置位置、字段及凭据优先级；
- 保护 Continue secrets 引用、dsh 环境变量、Pi `auth.json` / 环境变量 / 命令取密和 OpenCode BYOK 托管来源，避免无效或越权覆盖；
- 将 VS Code 扩展安装绑定到用户确认的扩展目录，并验证扩展身份；
- 增加跨进程写锁、预览配置摘要、写后复读验证、SQLite WAL 快照、DPAPI 加密备份和打包版手动恢复；
- 加固本地页面访问令牌、Host / Origin 校验、一次性预览及安装令牌、请求大小和 CSP；
- 加固下载重定向、哈希、ZIP 路径、Windows 设备名、重复项、符号链接及资源上限；
- 关闭窗口或服务时等待正在进行的写入或安装结束，测试失败也会精确清理自身进程树；
- 隔离不参与产品的本机逆向研究脚本和旧截图，防止误发布；
- 持续集成现在会执行依赖审计、构建 EXE，并对打包程序进行九目标写入/恢复验收。

## 验收证据

- 单元测试：70 / 70 通过；
- Web 冒烟、Python 编译、JavaScript 语法、Ruff、Bandit、依赖一致性：全部通过；
- 公开漏洞库依赖审计：未发现已知漏洞；
- 打包 E2E：9 个目标、15 处变更、DPAPI 备份恢复通过；
- 浏览器层 QA：主题、安装确认、预览、宽窄屏、控制台和退出流程通过；
- 真实桌面只读发现：9 个目标均正常返回，无读取失败；当前未安装 dsh 与 Pi 属于正常“未发现”状态；
- 三轮打包测试结束后均确认没有残留 `Key-Router.exe` 进程；
- 最终文件版本：0.6.0；SHA-256：`EB845A4D7C830720ED09B7745EB3603CF08655BF2F03A43316772D5E9FC79B54`。

## 已知交付边界

- 最终 EXE 未使用商业代码签名证书，Windows 可能显示未知发布者；随附 SHA-256 文件可用于完整性核对；
- 本机 Windows Defender 的命令行扫描功能处于禁用状态，因此无法完成本机杀毒扫描；命令返回的是“Product/Feature disabled”，不是检出威胁；
- 未对主人未安装的 dsh 与 Pi 做真实账户写入测试；二者已由隔离 E2E 覆盖安装发现、配置写入、备份和恢复逻辑；
- 本报告记录的是发布前本地验收；GitHub 发布结果以 Release 页面及发布资产回下载验证为准。
